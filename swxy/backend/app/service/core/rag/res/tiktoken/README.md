# Tiktoken vocabulary

The hash-named file in this directory is the bundled `cl100k_base` vocabulary
required by `tiktoken`. It is shipped as a runtime resource so token counting
works offline; it is not generated user data.
